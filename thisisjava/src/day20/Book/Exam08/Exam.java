package day20.Book.Exam08;

import java.util.Arrays;
import java.util.List;
import java.util.Map;
import java.util.stream.Collectors;

import day20.Book.Exam07.Member;

public class Exam {

//	public static void main(String[] args) {
//		List<Member> list = Arrays.asList(
//				new Member("홍길동", "개발자"),
//				new Member("김나리", "디자이너"),
//				new Member("신용권", "개발자")
//				);
//				
//		Map<String, List<Member>> groupingMap = list.stream()
//				.collect(Collectors.groupingBy(m->m.getJob().toString()
//						
//						));
//
//		System.out.println("[개발자]");
//		Map<String,String> joblist = list.stream()
//				.collect(Collectors.toMap(m->m.getName(), m->m.getJob()))
//				.forEach(m -> sysout);
////		.collect(Collectors.groupingBy(m->m.getName(),Collectors.groupingBy(m->m.toString())));
//		System.out.println();
//		
		
		
//	}
//
}
